{
  "name": "proyecto",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "deno run --allow-env --allow-net --allow-read app.js",
    "build": "webpack --mode production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "three": "^0.151.3"
  }
}
